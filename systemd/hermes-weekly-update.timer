[Unit]
Description=Run scheduled Hermes Agent updates

[Timer]
# The installer writes the user-selected calendar in schedule.conf.
# Fresh installations leave this timer disabled (on demand).
OnCalendar=daily
Persistent=true
AccuracySec=1min
Unit=hermes-weekly-update.service

[Install]
WantedBy=timers.target
